{
  "mcpServers": {
    "contextkeeper": {
      "type": "stdio",
      "command": "dotnet",
      "args": ["run", "--project", "/absolute/path/to/contextkeeper-mcp/src/ContextKeeper/ContextKeeper.csproj"],
      "env": {
        "DOTNET_CLI_TELEMETRY_OPTOUT": "1",
        "MCP_DEBUG": "true"
      },
      "description": "ContextKeeper MCP server with C# code search capabilities"
    }
  }
}